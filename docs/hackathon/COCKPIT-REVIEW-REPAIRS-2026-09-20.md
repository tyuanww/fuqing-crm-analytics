# 驾驶舱候选审查与修复 · 2026-09-20

本轮由用户点名 `open-code-review` 并授权修复，继续在 `codex/cockpit-history-import`、HEAD `f9070431` 处理累计未提交成果。范围为历史交付、手动文件接入、Office 编辑及原生 AI 候选。未提交、推送、建 PR、合并或切换现役6677；主仓 WeKnora 改动和固定上游保留。

## 审查执行

- 使用 OCR 1.12.4 的 workspace 模式，包含未跟踪的新代码。选择42个代码/合同/测试文件；排除生成图片与T0证据。OCR默认不审Markdown、`.d.mts`和生成类型，这些由本地差异/契约/类型检查补充。
- 首次运行42项全部因网络传输失败，0模型token；不能计为审查通过。原始结果保存在 `.context/ocr-20260920/review.json`。
- 按技能排障步骤核验系统代理7897，仅对OCR进程设置代理；`ocr llm test`成功后重新运行，保留原先配置的xAI/Grok审查模型，没有改变产品模型配置。第二次运行31分4秒后结束，**PARTIAL：完成18/42，24项超时，0条最终评论**。命令虽然退出0，结构化manifest明确为partial，不能当作全量审查通过。结果在`review-proxy.json`，session为`755cf086-da5c-4c24-a560-8ccb705b0a80`。
- 未完成的四组是AI候选6文件、Office保存7文件、历史识别4文件和驾驶舱接线7文件。记录含2条工具失败、响应解码阶段网络重试以及6次请求超时；未因部分组没有评论就认定剩余代码无缺陷。上述范围由本地源码核对、明确失败复现和回归验证补充，**不等价于OCR全部完成**；本轮未继续盲目重跑外部调用。
- 修改前对61个已有dirty/未跟踪文件保存hash清单与备份，未包含忽略的凭据或运行时。未stash/reset，也没有改动主仓工作树。

## 本地核实与修复

| 编号 | 级别与问题 | 修复和证据 |
|---|---|---|
| R1 | 高：撤销页面数据权限后，AI列表和相同id的begin重试仍返回任务与工作目录 | 两个入口重新核验页面当前权限；列表过滤失权任务。回归覆盖列表不可见和重试403。 |
| R2 | 中：页面AI候选保留超过30分钟后，confirm持续PREVIEW_EXPIRED，collect仍返回READY，无法继续 | 从已固化候选续建预览，保留hash和CAS；先持久化新preview id再调用页面库确认。覆盖跨库中断、重启重试和新版不可覆盖。 |
| R3 | 中：Office客户端只看SAVED，可把其他请求或非法版本号当作保存成功并清除dirty | 校验回执id、edit_key、终态与版本号；错配仍锁定并重试同一id。真实浏览器注入错配回执后恢复通过。 |
| R4 | 高：forcesave响应丢失后重试返回error 4，旧自动草稿会被误标为本次已保存 | 删除旧草稿回退；只有带本次userdata的签名回调可写正式版本。编辑器内部保存仅同步，驾驶舱发起带回执id的强制保存。新增回归修复前红、修复后绿。 |
| R5 | 中：另一编辑先推进版本后，旧编辑的CAS失败没有终态回执，界面会永久视作保存未知 | 持久化CONFLICT回执，前端保留修改并提示下载备份后显式放弃/刷新。真实浏览器验证v7编辑不能覆盖v8，冲突后可正常退出。 |
| R6 | 中：AI任务列表只返回100条且读取全部source/candidate BLOB | 元数据查询不读取大文件内容，增加分页并由客户端逐页恢复；101任务和非法分页回归通过。 |
| R7 | 中：重复begin先查再插缺少串行事务 | 使用BEGIN IMMEDIATE保护相同id的创建，双线程回归确认只产生一个任务。 |

Office协议核对依据：[强制保存](https://api.onlyoffice.com/docs/docs-api/additional-api/command-service/forcesave/)、[回调userdata](https://api.onlyoffice.com/docs/docs-api/usage-api/callback-handler/)、[命令错误码](https://api.onlyoffice.com/docs/docs-api/additional-api/command-service/)。错误码4不构成应用侧的持久化证明。

## 验证记录

- 原有针对性基线：47项Python、17项Node通过，但没有覆盖上述边界。
- 第一批修复：52项Python、19项Node通过；B0完整pipeline通过，包含560项Python、所选Node/编译后DOM、契约、类型、构建和干净重建。之后增加R4/R5，不能把这一轮全套成绩冒认为最终版本成绩。
- R4/R5针对性：39项文件/AI后端及12项AI/文件客户端测试通过；Ruff与diff空白检查通过。
- 真实固定DSH 4328 + 文件服务19091 + Docker ONLYOFFICE18110：Word正常保存v5、丢失响应恢复v6、错配回执恢复v7；错误回执时选择及编辑锁定，重试id相同。独立SQLite连接和DOCX XML解析确认v1–v7保留及各次正文确实落盘。
- 冲突浏览器：独立合成AI候选HTTP路径生成v8，再让基于v7的Office保存。收到CONFLICT、编辑不再被inert锁死，可显式放弃后刷新v8；独立python-docx解析确认新正文仍在、旧编辑正文未覆盖。
- OCR工具警告所涉及的上游入口另行核对：固定上游`client/connection`将共享`/api`放在身份/Origin围栏后，`session-query`的observe/list接口与当前接线一致。真实4328未带凭据请求历史交付接口返回401，证据`history-auth.json`；原生历史文件读取已在同一测试profile验证。
- 浏览器主流程无未处理异常。测试期间主动重启隔离Host导致旧浏览器页出现两条原生adapter/session错误，已单独保留；重新进入后的冲突主链无异常。初次自动化未等待模型配置弹窗，出现点击超时，选择“稍后配置”后通过；不是模型生成验收。
- 最终完整B0 pipeline通过：562项Python、所选Node/编译后DOM、契约、类型检查、所有插件构建及干净重建；见`pipeline-final.log`。运行时新生成的T0图片/JSON另存本轮证据，仓库内原有未提交T0文件按施工前hash原样恢复。全部原始日志、截图、落盘断言在 `.context/ocr-20260920/`，失败记录未删除。

## 未通过或未执行

OCR全量审查未完成，仍缺上述24项外部审查结论。R1–R7为本地核实并修复的结果，不冒充Grok输出的审查意见；列表与并发部分同时包含有针对性的健壮性加固。本轮停在本地可审查状态，不能据此跳过后续已授权发布时所需的检查点。

真实模型修改、P13、用户真实历史/业务文件、用户UAT、远端CI与现役切换均不在本轮通过范围。浏览器中并发的新版本由合成脚本通过真实候选接口生成，模型调用数为0。测试profile缺少业务模型凭据的既有失败保留；OCR能调用审查模型不代表产品的原生模型已配置。

扫描PDF OCR、复杂Office/旧格式兼容及PDF浮动菜单的上游异常仍按前次报告开放。新修复未扩大数据绑定HTML的AI权限，仍只允许CSS变更。预览服务保持在本轮独立端口供查看。
