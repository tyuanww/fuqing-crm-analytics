# 真实指标接入与待核对项

2026-09-20追加确认：**真实 AUS、AOV 的金额分子沿用当前看板 GSV；扣实际退款的净额版单列。** AUS 除同范围去重有效购买人数，AOV 除同范围去重有效订单数。此确认不改变 `crm-metrics/v1` 净额候选的定义，也不授权用旧字段冒充新指标。

## 当前能证实什么

| 项目 | 证据与结论 | 下一项验收 |
|---|---|---|
| 真实看板 GSV | #216后已通过登录后的真实模型工具调用；一组真实窗口金额、趋势及先前只读复算闭合 | 扩充窗口/渠道覆盖；水位和退款截止日仍未知 |
| AOV | 现 overview 提供同过滤的 `COUNT(DISTINCT order_id)`；此前真实窗口与独立复算订单数一致。可按 GSV÷去重订单复核 | 隔离候选已提供同次聚合及AI工具；需完成发布、核对真实未知订单号/零额订单与分母覆盖 |
| AUS | 现 overview 没有完整去重购买人数及未知身份金额；`new_users + old_users` 依赖首购表，缺首购记录者可能漏计 | 隔离候选已增补同GSV范围总去重买家、未知行数/金额和覆盖标记；仍需发布并做真实闭合 |
| 会员溢价 | 分母已确认是非会员AUS；真实 `is_member` 有历史补标/回填，不是可靠成交时身份 | 需要入会/退会事件或订单成交时身份快照；未知单列，不从当前身份倒推 |
| 净额版GSV | 已实现并通过合成验证，真实退款字段有异常；与看板版不同 | 真实退款流水、成功时间、父/子订单归属、来源快照及完整性 |
| 新老客/复购/LTV | 合成计算具备已确认规则，旧首购缓存不能按退款截止日重算 | 全店身份域、历史覆盖、水位和退款历史 |
| 派样ROI | 仍仅表示派样后的复购收入表现，观察从支付日起 | 派样资格、首笔归属、成熟观察期及订单关联；利润ROI仍缺成本 |

`backend/services/metrics/overview.py::_query_period` 的 `avg_order_value` 是 **AVG(actual_amount) 明细行均值**，不能当 AOV。现会员溢价的比较对象和身份时点也不能直接复用。返回“—”及原因比填0更准确。

## 数据接入施工边界

1. 通过现有认证指标服务扩展受控聚合合同；模型继续只传时间、渠道和低价条件，不获得 SQL 或数据库路径。
2. 复用同一 GSV 过滤定义；在一组聚合内返回金额、去重订单/买家数，以及未知身份/订单金额。分母覆盖不足时不输出误导性的均值，不用新老客人数相加替代完整买家数。
3. 明确零额订单、负金额/异常退款和未知键的处理，先用多行订单、未知用户、全退/部分退、跨渠道、零分母等合成夹具验证。源字段无法支持的能力留不可用。
4. 真实阶段限定已确认日期与权限，优先已有服务；归档库仍不复制、迁移、回填或改写。只读连接、锁及资源预算须另核验。合成适配器不放宽大小限制去接131GiB归档。
5. 当前无会员事件/退款事件表；先补来源证明和最小必要上游明细，不能只增加数据库空列就宣布接通。

后续核对只针对实质差异。当前看板 GSV 优先级、AUS/AOV分母、会员溢价比较对象等已经确认的原则不重复问。数据来源、能力上线、真实业务验收分别记状态。
